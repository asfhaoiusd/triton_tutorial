# Triton 讲义

编译好的 PDF：[`main.pdf`](main.pdf)。

版式对齐 Stanford CS336 assignment handout（article、10pt、蓝/橙提示框）。代码只摘仓库关键片段。

```bash
cd docs/textbook
make
make clean
```

需要 XeLaTeX + latexmk + ctex。节次：AI Infra / CUDA → Triton 语法 → 01--07 算子 → Qwen 推理 → llm\_train。
